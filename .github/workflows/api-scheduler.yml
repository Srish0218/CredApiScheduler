# .github/workflows/api-caller.yml
name: Call API every 5 mins

on:
  schedule:
    - cron: "*/5 * * * *"  # Every 5 minutes
  workflow_dispatch:

jobs:
  call-api:
    runs-on: ubuntu-latest
    steps:
      - name: Call your API
        run: curl -X GET "https://cred-testapi.transmonqa.in:9443/brcp"
